```javascript
    const arr = [];
    arr.push("Emmanuel")
    arr.push("Otobo")
    arr.unshift("Blue")

    // 
    arr.shift();
    const arr2 = []
    arr2.push(42)
    arr2.push("JavaScript")

    // 
    const exists = arr2.indexOf(42);
    const combinedArr = arr.concat(arr2);
```